/**
 * MSAL Configuration — Stub module.
 *
 * Azure AD integration is optional. To enable:
 *   1. npm install @azure/msal-browser @azure/msal-react
 *   2. Set VITE_AZURE_AD_CLIENT_ID and VITE_AZURE_AD_TENANT_ID
 *   3. Replace this stub with the real MSAL configuration
 */

export const msalConfig = {};
export const loginRequest = { scopes: [] };
export const msalInstance = null;
